[Unit]
Description=Lambada Web (scan listing server)
After=network.target

[Service]
Type=simple
User=pi
WorkingDirectory=/home/pi/workspace/lambada
ExecStart=/usr/bin/env ./lambada-web
Restart=always

[Install]
WantedBy=multi-user.target
