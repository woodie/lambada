{
  "name": "lambada",
  "private": true,
  "description": "Lint/test task runner for lambada (JS via npm scripts, Go via golangci-lint/ginkgo)",
  "scripts": {
    "test": "vitest run && ginkgo-fd -r",
    "lint-js": "standard",
    "test-js": "vitest run",
    "lint-go": "golangci-lint run",
    "test-go": "ginkgo-fd -r",
    "lint-html": "html-validate --ext=tmpl cmd/lambada-web/views/",
    "check": "npm run lint-js && vitest run --reporter=dot && npm run lint-go && LOG_LEVEL=OFF ginkgo -r && npm run lint-html"
  },
  "devDependencies": {
    "html-validate": "^11.5.6",
    "sinon": "^19.0.2",
    "standard": "^17.1.2",
    "vitest": "^4.1.10"
  },
  "standard": {
    "env": [
      "browser"
    ],
    "globals": [
      "context"
    ]
  }
}
